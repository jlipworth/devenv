# LaTeX layer
# Install: brew bundle --file=brewfiles/Brewfile.latex

brew "texlab"
brew "poppler"
brew "aspell"

# PDF viewer with SyncTeX support for VimTeX forward/inverse search
cask "skim" if OS.mac?
