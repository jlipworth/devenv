## CLI Tool Availability
This environment often has modern CLI tools installed, including `rg` (ripgrep), `fd`, `eza`, `bat`, `dust`, `sd`, `procs`, and `zoxide`.
Use whichever tools are most appropriate for the task rather than forcing replacements for standard Unix utilities.
